const gemaEspacio = require ("./gemaEspacio")
const gemaMente = require ("./gemaMente")
const gemaTiempo = require ("./gemaTiempo")


const gemaEspacio = new gemaEspacio();
const gemaMente = new gemaMente();
const gemaTiempo = new gemaTiempo();

function usarGema(obj) {
    if (obj) {
        
    }
}
